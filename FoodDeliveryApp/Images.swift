// Images.swift
// Copyright © Darkness Production. All rights reserved.

import UIKit

struct Images {
    static let menuTabbar = UIImage(named: "menu") ?? UIImage()
    static let contactsTabbar = UIImage(named: "contacts") ?? UIImage()
    static let profileTabbar = UIImage(named: "profile") ?? UIImage()
    static let cartTabbar = UIImage(named: "cart") ?? UIImage()
}
